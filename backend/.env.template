PORT=3000


#Dev set up
PROD=false

JWT_SEED=cualquier_cosa

MONGO_URL=mongodburl
MONGO_DB_NAME=MFLOW 
MONGO_USER=user
MONGO_PASS=password

SEND_EMAIL=false
# Resend (https://resend.com) — required when SEND_EMAIL=true
RESEND_API_KEY=""
# Verified domain sender, e.g. MFLOW <noreply@yourdomain.com> or onboarding@resend.dev for tests
RESEND_FROM_EMAIL=no-reply@mflow.tech
